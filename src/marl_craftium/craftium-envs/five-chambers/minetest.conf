# Five Chambers – Minetest/Luanti server config
time_speed = 0
enable_damage = true
creative_mode = false
dedicated_server_step = 0.05
enable_client_modding = true
csm_restriction_flags = 0
enable_mod_channels = true
server_name = five-chambers

# Containment: prevent agents escaping rooms
free_move = false
noclip = false
give_initial_stuff = false

# Disable mods unused by five-chambers. mcl_fire's spread ABM has a
# shutdown race (random(humidity_factor) with 0 humidity → "interval is
# empty") that prints a noisy ERROR every episode end. The chambers have
# no fire mechanics, so the mod is pure overhead. lightning hard-depends
# on mcl_fire and is also irrelevant (indoor chambers, time_speed = 0).
load_mod_mcl_fire = false
load_mod_lightning = false
